<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>VideoLocalShow</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<!-- Must be the fully-namespaced type name, not just the bare class name - with only
		     "ShareViewController" here, iOS silently fails to resolve the class on a physical
		     device (this exact symptom: appears in the share sheet, does nothing when tapped) -
		     even though it happens to still resolve fine in the Simulator without it. -->
		<key>NSExtensionPrincipalClass</key>
		<string>VideoLocalShow.ShareExtension.ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Accepts both an actual URL attachment (e.g. shared from Safari's address bar)
			     and plain text (how YouTube's own share sheet hands off a video link) - either
			     is enough to trigger this extension appearing in "Condividi". -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
